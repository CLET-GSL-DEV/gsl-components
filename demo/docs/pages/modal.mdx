export const meta = {
  title: "Modal",
  description: "Centered overlay with size variants",
  category: "Components",
  keywords: ["modal", "overlay", "review dialog", "full screen modal", "workflow modal"],
  since: "1.7.0",
  status: "stable",
  related: ["dialog", "sheet"],
  import: "@rfdtech/components",
};

import { ExampleTabs } from "../components/ExampleTabs";
import { ModalPreview } from "../previews/code/ModalPreview";
import modalSource from "../previews/examples/modal.example.tsx?raw";

# Modal

Compound modal primitives for centered overlays with header, scrollable body, and footer regions. Built on Radix Dialog. Comes in four sizes with independently customizable max-width tokens.

Use **Modal** for large workflows or review panels. Use [**Dialog**](/docs/dialog) for smaller centered modals.

## Example

<ExampleTabs
  title="Review changes"
  preview={<ModalPreview />}
  code={modalSource}
/>

Click **Review changes** to open the modal with `?modal=review-changes`. Close the modal or use the browser back button to clear the params.

## Sizes

| Size | Token | Max width |
|------|-------|-----------|
| `sm` | `--clet-modal-max-width-sm` | 400px |
| `md` | `--clet-modal-max-width-md` | 512px |
| `lg` (default) | `--clet-modal-max-width-lg` | 640px |
| `xl` | `--clet-modal-max-width-xl` | 800px |
| `2xl` | `--clet-modal-max-width-2xl` | 1024px |

Override any size token to customize without touching the CSS classes:

```css
:root {
  --clet-modal-max-width-lg: 720px;
}
```

## Prevent close

Pass `preventClose` to intercept the X button, overlay click, and Escape key. A confirmation dialog appears before closing. Useful when the modal contains unsaved changes.

Customize the confirm dialog text with `preventCloseTitle` and `preventCloseDescription`.

```tsx
<ModalContent
  showCloseButton
  size="xl"
  preventClose={form.formState.isDirty}
  preventCloseTitle="Discard changes?"
  preventCloseDescription="You have unsaved changes. Are you sure you want to close?"
>
```

## URL state

Sync open state with URL search params using [`useModalSearchParam`](/docs/hooks). The example above wires `open` and `onOpenChange` from the hook and opens via `openWith()`.

| URL | Meaning |
|-----|---------|
| `?modal=review-changes` | Modal `review-changes` is open |
| `?modal=bulk-import` | Another modal id (e.g. `BulkImportModal`) |

Use a `Button` with `openWith()` instead of `ModalTrigger` when open state is URL-controlled. For React Router or other routers, pass a custom `adapter` — see [Hooks](/docs/hooks#react-router).

## Components

### `Modal`

Root wrapper. Accepts Radix `Dialog.Root` props such as `open`, `defaultOpen`, and `onOpenChange`.

### `ModalTrigger`

Wraps the element that opens the modal. Use `asChild` to merge props onto a child such as `Button`.

### `ModalPortal`

Renders overlay and content in a portal. Wrap `ModalOverlay` and `ModalContent` inside the portal.

### `ModalOverlay`

Styled backdrop behind the modal shell.

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `classNames` | `ModalOverlayClassNames` | — | Part-level class overrides |
| `className` | `string` | — | Merged onto the overlay element |

Also accepts remaining Radix `Dialog.Overlay` props.

### `ModalContent`

Centered modal shell with size variants, optional close control, and close-prevention.

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `showCloseButton` | `boolean` | `false` | Shows a top-right close control with an accessible label |
| `size` | `"sm" \| "md" \| "lg" \| "xl" \| "2xl"` | `"lg"` | Controls max-width via the corresponding `--clet-modal-max-width-*` token |
| `preventClose` | `boolean` | `false` | When true, intercepts close attempts and shows a confirmation dialog |
| `preventCloseTitle` | `string` | `"Discard changes?"` | Title for the close-confirmation overlay |
| `preventCloseDescription` | `string` | `"You have unsaved changes..."` | Description for the close-confirmation overlay |
| `onOpenChange` | `(open: boolean) => void` | — | Pass the parent's close handler for direct close-control (required when using `preventClose`) |
| `classNames` | `ModalContentClassNames` | — | Part-level class overrides |
| `className` | `string` | — | Merged onto the shell element |

Also accepts remaining Radix `Dialog.Content` props.

### `ModalHeader`

Top chrome bar for titles and supporting text.

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `classNames` | `ModalHeaderClassNames` | — | Part-level class overrides |
| `className` | `string` | — | Merged onto the header element |

### `ModalTitle`

Accessible modal heading. Render inside `ModalHeader`.

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `classNames` | `ModalTitleClassNames` | — | Part-level class overrides |
| `className` | `string` | — | Merged onto the title element |

### `ModalDescription`

Optional supporting text below the title.

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `classNames` | `ModalDescriptionClassNames` | — | Part-level class overrides |
| `className` | `string` | — | Merged onto the description element |

### `ModalBody`

Scrollable main content area.

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `classNames` | `ModalBodyClassNames` | — | Part-level class overrides |
| `className` | `string` | — | Merged onto the body element |

### `ModalFooter`

Bottom action bar. Defaults to end-aligned flex layout for buttons.

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `classNames` | `ModalFooterClassNames` | — | Part-level class overrides |
| `className` | `string` | — | Merged onto the footer element |

### Other exports

| Export | Radix primitive | Notes |
|--------|-----------------|-------|
| `ModalClose` | `Close` | Closes the modal when activated; used by the built-in close button and custom actions |

## Types

### `ModalSize`

`"sm" | "md" | "lg" | "xl" | "2xl"`

### `ModalOverlayProps` / `ModalContentProps` / `ModalHeaderProps`

Extend the corresponding element or Radix props with optional `classNames`.

### `ModalTitleProps` / `ModalDescriptionProps`

Extend Radix title and description props with optional `classNames`.

### `ModalBodyProps` / `ModalFooterProps`

Extend native `div` / `footer` attributes with optional `classNames`.

### `ModalOverlayClassNames`

| Key | Applied to |
|-----|------------|
| `overlay` | Overlay element (`.clet-modal__overlay`) |

### `ModalContentClassNames`

| Key | Applied to |
|-----|------------|
| `content` | Shell element (`.clet-modal`) |
| `close` | Built-in close button (`.clet-modal__close`) |

### `ModalHeaderClassNames` / `ModalTitleClassNames` / `ModalDescriptionClassNames`

| Type | Key | Applied to |
|------|-----|------------|
| `ModalHeaderClassNames` | `header` | Header element (`.clet-modal__header`) |
| `ModalTitleClassNames` | `title` | Title element (`.clet-modal__title`) |
| `ModalDescriptionClassNames` | `description` | Description element (`.clet-modal__description`) |

### `ModalBodyClassNames` / `ModalFooterClassNames`

| Type | Key | Applied to |
|------|-----|------------|
| `ModalBodyClassNames` | `body` | Body element (`.clet-modal__body`) |
| `ModalFooterClassNames` | `footer` | Footer element (`.clet-modal__footer`) |

## Design tokens

| Token | Default | Used for |
|-------|---------|----------|
| `--clet-modal-font` | `var(--clet-font-body)` | Font family override for the modal shell |
| `--clet-modal-confirm-font` | `var(--clet-font-body)` | Font family override for the `preventClose` confirmation dialog |
| `--clet-modal-gutter` | `24px` | Viewport gutter reserved around the modal shell when sizing its width/height (set to `0` below 768px, where the modal fills the viewport) |

See [Sizes](#sizes) above for the `--clet-modal-max-width-*` tokens.

## Notes

- The modal is centered in the viewport with `top: 50%; left: 50%; transform: translate(-50%, -50%)`.
- Border and shadow match the popover style (`1px solid var(--clet-border)`, `var(--clet-shadow-sm)`, `var(--clet-radius-xl)`).
- Default size is `lg` (640px). Override per-size max-widths via `--clet-modal-max-width-{size}` tokens.
- On mobile (≤768px) the modal fills the viewport without gutters or border-radius.
- `ModalBody` scrolls internally when content exceeds the available height.
- The built-in close control uses a 40×40px hit area for touch-friendly dismissal.
- `preventClose` shows a confirmation dialog before closing. Pass `onOpenChange` to `ModalContent` for close-control.
- Portaled content inherits the active theme via `data-clet-theme` on `document.documentElement` when using `ThemeProvider`.
- Enter/exit animations are disabled when `prefers-reduced-motion: reduce` is set.
- Base `clet-modal*` classes are always applied; `classNames` and `className` append consumer classes for Tailwind utilities.
- **URL-driven open state** is the recommended pattern: use `useModalSearchParam` with `open`, `onOpenChange`, and `openWith` (see [URL state](#url-state) above).

## Do

- Follow the structure `Modal > ModalPortal > ModalOverlay + ModalContent(showCloseButton, size) > ModalHeader(ModalTitle, ModalDescription) > Form > ModalBody > ModalFooter`.
- Give `Field`/`Textarea` inside `ModalBody` a `className="w-full"` so they fill the width.
- Use `bg-surface-muted/20` (not plain `bg-surface-muted`) for subtle card backgrounds inside a modal.

## Don't

- Wrap the form contents in a literal `<form>` element; call `form.handleSubmit(onSubmit)` from the submit button's `onClick` instead.
