export const meta = {
  title: "Card",
  description: "Surface card container",
  category: "Components",
  keywords: ["card", "surface", "container", "panel", "content wrapper"],
  since: "1.9.0",
  status: "stable",
  related: ["metric-card", "badge"],
  import: "@rfdtech/components",
};

import { ExampleTabs } from "../components/ExampleTabs";
import { CardPreview } from "../previews/code/CardPreview";
import cardSource from "../previews/examples/card.example.tsx?raw";

# Card

Surface card wrapper for grouping content. Uses `--gsl-surface-card` for background and `--gsl-card-padding` for spacing — both overridable via CSS custom properties or `className`.

## Example

<ExampleTabs title="Card variants" preview={<CardPreview />} code={cardSource} />

## Card

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `children` | `ReactNode` | — | Card content |

Also accepts remaining `HTMLAttributes<HTMLDivElement>`.

## CardHeader

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `children` | `ReactNode` | — | Header content |

Also accepts remaining `HTMLAttributes<HTMLDivElement>`.

## CardTitle

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `children` | `ReactNode` | — | Title text or element |

Also accepts remaining `HTMLAttributes<HTMLHeadingElement>`.

## CardActions

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `children` | `ReactNode` | — | Action items (buttons, links) |

Also accepts remaining `HTMLAttributes<HTMLDivElement>`.

## Design tokens

| Token | Fallback | Used for |
|-------|----------|----------|
| `--gsl-surface-card` | `--gsl-bg` | Card background |
| `--gsl-card-padding` | `20px` | Inner padding (16px on mobile) |
| `--gsl-card-font` | `--gsl-font-body` | Card font family |
| `--gsl-card-title-font` | `--gsl-font-header` | `CardTitle` font family |

Override padding via `--gsl-card-padding` or tailwind padding classes through `className`.

## Notes

- On mobile (`<= 768px`), padding reduces to 16px via `--gsl-card-padding`.
- Use `className` to override background, border-radius, shadow, or any root style.

## Do

- For scrollable card content, use `Card className="flex flex-col"` with an inner `flex-1 overflow-y-auto min-h-0` region — `min-h-0` is required for the flex-overflow scroll to work.

## Don't

- Add explicit padding (`px-6`, `py-4`) inside Card content — Card has a built-in `--gsl-card-padding` token.
- Add `border-t` on card footers — keep the flow borderless.
