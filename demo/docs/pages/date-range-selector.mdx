export const meta = {
  title: "DateRangeSelector",
  description: "Date range picker",
  category: "Components",
  keywords: ["date range", "date range picker", "calendar range", "start and end date", "date filter"],
  since: "1.9.0",
  status: "stable",
  related: ["date-selector", "table", "form-field"],
  import: "@rfdtech/components",
};

import { ExampleTabs } from "../components/ExampleTabs";
import { DateRangeSelectorPreview } from "../previews/code/DateRangeSelectorPreview";
import { DateRangeFilterPreview } from "../previews/code/DateRangeFilterPreview";
import dateRangeSource from "../previews/examples/date-range-selector.example.tsx?raw";
import dateRangeFilterSource from "../previews/examples/date-range-filter.example.tsx?raw";

# DateRangeSelector

Date range picker for choosing a start and end date from a two-month calendar popover. Built on `@radix-ui/react-popover` with GSL styling, auto-swap to keep start before end, year/month dropdown selectors for quick navigation, Apply/Cancel buttons for explicit confirmation, and part-level `classNames`.

## Example

<ExampleTabs
  title="Basic date range"
  preview={<DateRangeSelectorPreview />}
  code={dateRangeSource}
/>

## Inside a table filter

`DateRangeSelector` used inside `TableFilter` to drive a table query, with an in-place empty
state and an in-place error state (swap `TableContent` for an inline message on genuine errors —
don't let it silently render an empty table).

<ExampleTabs
  title="Date range filter on a table"
  preview={<DateRangeFilterPreview />}
  code={dateRangeFilterSource}
/>

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `value` | `DateRangeValue` | — | Controlled range (`{ start, end }`) |
| `defaultValue` | `DateRangeValue` | — | Default range when uncontrolled |
| `onChange` | `(range: DateRangeValue) => void` | — | Called when the range is confirmed via Apply |
| `placeholder` | `string` | `"Select date range"` | Shown in the trigger when no dates are selected |
| `formatOptions` | `Intl.DateTimeFormatOptions` | `{ year: "numeric", month: "short", day: "numeric" }` | Controls how dates are formatted in the trigger display |
| `invalid` | `boolean` | `false` | Show error border styling |
| `disabled` | `boolean` | `false` | Disable interaction |
| `min` | `Date` | — | Earliest selectable date |
| `max` | `Date` | — | Latest selectable date |
| `classNames` | `DateRangeSelectorClassNames` | — | Part-level class overrides |
| `className` | `string` | — | Merged onto the root wrapper |

Also accepts remaining `HTMLAttributes<HTMLDivElement>`, excluding `onChange`, `value`, and `defaultValue`.

## Usage with react-hook-form

```tsx
import { useForm } from "react-hook-form";
import { Form, FormField, Field, FieldControl, FieldLabel, FieldError, DateRangeSelector } from "@rfdtech/components";
import type { DateRangeValue } from "@rfdtech/components";

function FilterForm() {
  const form = useForm<{ dateRange: DateRangeValue }>({
    defaultValues: { dateRange: { start: null, end: null } },
  });

  return (
    <Form {...form}>
      <form onSubmit={form.handleSubmit((d) => console.log(d.dateRange))}>
        <FormField
          control={form.control}
          name="dateRange"
          render={({ field, fieldState }) => (
            <Field invalid={!!fieldState.error}>
              <FieldLabel>Date range</FieldLabel>
              <FieldControl>
                <DateRangeSelector value={field.value} onChange={field.onChange} />
              </FieldControl>
              <FieldError>{fieldState.error?.message}</FieldError>
            </Field>
          )}
        />
      </form>
    </Form>
  );
}
```

## Types

### `DateRangeValue`

Value shape for `DateRangeSelector`. Holds the selected start and end dates, each `null` when unset. Start is always the earlier date.

| Key | Type | Description |
|-----|------|-------------|
| `start` | `Date \| null` | Earlier date in the range (always the smaller date) |
| `end` | `Date \| null` | Later date in the range (always the larger date) |

### `DateRangeSelectorProps`

See the props table above.

### `DateRangeSelectorClassNames`

Part-level class names merged after base `gsl-date-selector*` styles:

| Key | Applied to |
|-----|------------|
| `root` | Outer wrapper (`.gsl-date-range-selector`) |
| `trigger` | Trigger button (`.gsl-date-selector__trigger`) |
| `calendar` | Calendar panel (`.gsl-date-selector__calendar`) |
| `calendarHeader` | Month/year navigation row (`.gsl-date-selector__calendar-header`) |
| `calendarNav` | Previous/next month arrows (`.gsl-date-selector__calendar-nav`) |
| `calendarTitle` | Month label above each month panel (`.gsl-date-selector__calendar-title`) |
| `calendarWeekdays` | Weekday labels container (`.gsl-date-selector__calendar-weekdays`) |
| `calendarWeekday` | Individual weekday label (`.gsl-date-selector__calendar-weekday`) |
| `calendarGrid` | Calendar day grid (`.gsl-date-selector__calendar-grid`) |
| `calendarDay` | Individual day button (`.gsl-date-selector__calendar-day`) |
| `calendarFooter` | Apply/Cancel button row (`.gsl-date-selector__calendar-footer`) |
| `applyButton` | Apply button (`.gsl-date-selector__calendar-btn--apply`) |
| `cancelButton` | Cancel button (`.gsl-date-selector__calendar-btn--cancel`) |

## Design tokens

| Token | Default | Used for |
|-------|---------|----------|
| `--gsl-date-range-selector-font` | `--gsl-font-body` | Trigger button font family |
| `--gsl-date-selector-calendar-font` | `--gsl-font-body` | Calendar popover font family (header title, weekday labels, and day cells) |

## Notes

- Click the trigger to open a two-month calendar popover. Year and month dropdowns at the top allow quick navigation; previous/next arrows shift by one month.
- First click picks the start date; a second click picks the end date. **No dates are committed until Apply is clicked.**
- Click Cancel or press Escape to discard the pending selection and restore the previous range.
- **Start is always the earlier date**—if the second pick is before the first, they swap automatically so `start < end`.
- Selecting the same day for both picks produces a single-day range where `start === end`.
- Clicking a new date when a range is already set clears the pending range and starts a new selection.
- Days strictly between start and end are highlighted with a subtle background in the calendar.
- The Apply button is disabled until at least a start date is selected.
- On mobile (below 768px), the two months stack vertically.

## Don't

- Don't override the trigger's border with class overrides like `classNames={{ trigger: "!border-0 !shadow-none focus:!shadow-none" }}` — let it keep its default border, same as `DateSelector` and `Dropdown`.
